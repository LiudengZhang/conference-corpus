Let's discuss. So what if we want to include not only HRD, but also other clinical relevant genomic features, especially MSI? Think about how to broden our scope.
And secondly, I believe there would be a huge lacking of the data which have both HRD info and the scRNA seq; however, dataset like TCGA, should have many bulk-RNA + gemonic data; I wonder if that is possible for us to build a model, which can predict the genomic event, especially HRD and MSI, by using the transcriptomic data (including single cell RNAseq data) only? SO for training such a model, we would use TCGA data to train (if there are other good cohort for training I would be super glad to see), and use the limited cohorts which have both scRNA seq and HRD info as validation. Think about how can we train such a model, and thus we can include the scRNA-seq only cohort into our pan cancer analysis.
Write blog for both of them. And for papermap, add the function that I could mannually write blogs on that. 